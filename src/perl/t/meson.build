# Bsd-Perl Tests
#============================================================================


# src
#---------------------------------------------------

bsd_perl_tests = files(
  'init.t',
  # hack for trailing newline
)


foreach f : bsd_perl_tests
  fs.copyfile(f)
endforeach
